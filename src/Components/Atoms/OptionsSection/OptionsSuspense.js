import React from "react";
import Styles from "./styles.module.scss";

const OptionsSuspense = () => {
  return <div className={`${Styles.OptionsContainer} ${Styles.Animation}`} />;
};

export default OptionsSuspense;
