import React from "react";
import Styles from "./styles.module.scss";

const OrderSuspense = () => {
  return <div className={`${Styles.OrderContainer} ${Styles.Animation}`} />;
};

export default OrderSuspense;
