import React from "react";
import Styles from "./styles.module.scss";

const ReviewSuspense = () => {
  return <div className={`${Styles.ReviewContainer} ${Styles.Animation}`} />;
};

export default ReviewSuspense;
