import React from "react";
import Styles from "./styles.module.scss";

const Inventory = () => {
  return (
    <div className={Styles.InventoryContainer}>
      <header className={Styles.Header}>Inventory</header>
    </div>
  );
};

export default Inventory;
