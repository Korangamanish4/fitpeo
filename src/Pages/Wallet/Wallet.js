import React from "react";
import Styles from "./styles.module.scss";

const Wallet = () => {
  return (
    <div className={Styles.WalletContainer}>
      <header className={Styles.Header}>Wallet</header>
    </div>
  );
};

export default Wallet;
